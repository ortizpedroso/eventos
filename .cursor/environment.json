{
  "$schema": "https://www.cursor.com/schemas/environment.schema.json",
  "name": "EventosBR (FastAPI + Next.js)",
  "install": "bash .cursor/setup-cloud.sh",
  "start": "export PATH=\"$HOME/.local/bin:$PATH\"; command -v docker >/dev/null 2>&1 && (sudo service docker start 2>/dev/null || sudo systemctl start docker 2>/dev/null || true)",
  "terminals": [
    {
      "name": "api",
      "command": "export PATH=\"$HOME/.local/bin:$PATH\" && python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
      "description": "FastAPI na porta 8000 (SQLite local, ASAAS_DISABLED=true no .env)"
    },
    {
      "name": "web",
      "command": "npm run dev --prefix frontend",
      "description": "Next.js dev server na porta 3000"
    }
  ],
  "ports": [
    { "name": "api", "port": 8000 },
    { "name": "web", "port": 3000 }
  ]
}
