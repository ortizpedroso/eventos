import type { Metadata } from "next";

export const metadata: Metadata = { title: "Eventos" };

/** Conteúdo renderizado via OrganizadorPanelViews (montagem persistente — sem piscada). */
export default function OrganizadorEventosPage() {
  return null;
}
